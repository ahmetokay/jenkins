pipeline {
    agent {
        docker { image 'node:16.13.1-alpine' }
    }

    stages {
        stage('Git') {
            steps {
                sh 'node --version'

                sh "ls -lat"
            }
        }
    }
}
